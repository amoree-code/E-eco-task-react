import BlockRenderer from "@/components/BlockRenderer";
import Header from "@/components/Header";

const page = () => {
  return (
    <section>
      <Header />
      <BlockRenderer />
    </section>
  );
};

export default page;
